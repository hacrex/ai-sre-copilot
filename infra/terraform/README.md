# Terraform

Reserved for cloud infrastructure.

Planned modules:

- network
- Kubernetes cluster
- PostgreSQL
- Redis
- object storage
- observability
- secrets

Cloud implementations should live under `aws/` and `gcp/`.
